# Coding Platform Environment Configuration
# Copy this file to .env and update the values
# DO NOT commit .env file to version control!

# Database Configuration
DB_NAME=coding_platform
DB_USER=admin
DB_PASSWORD=your_secure_database_password_here
DB_PORT=5432

# Service Ports (Development)
BACKEND_PORT=8080
WORKER_PORT=8081
FRONTEND_PORT=3000

# AWS Configuration
# Get these from your AWS console or IAM user credentials
AWS_ACCESS_KEY_ID=AKIA________________
AWS_SECRET_ACCESS_KEY=________________________________
AWS_REGION=us-east-1

# SQS Configuration
# Format: https://sqs.{region}.amazonaws.com/{account-id}/{queue-name}
SQS_REQUEST_URL=https://sqs.us-east-1.amazonaws.com/YOUR_ACCOUNT_ID/coding-platform-requests
SQS_RESPONSE_URL=https://sqs.us-east-1.amazonaws.com/YOUR_ACCOUNT_ID/coding-platform-responses

# S3 Configuration
# Your S3 bucket for storing code submissions
S3_BUCKET_NAME=your-coding-platform-bucket

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WORKER_URL=http://localhost:8081

# Important Notes:
# 1. Replace all placeholder values with actual credentials
# 2. Never commit actual credentials to version control
# 3. Use strong passwords for database access
# 4. Create SQS queues and S3 bucket in AWS before running
# 5. Ensure AWS credentials have appropriate permissions