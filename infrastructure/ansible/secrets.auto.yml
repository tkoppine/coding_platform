# ================================
# SENSITIVE CONFIGURATION
# ================================
# IMPORTANT: Replace these placeholder values with your actual AWS resources

# SQS Queue URLs - Replace with your actual SQS queue URLs
sqs_request_url: "https://sqs.YOUR_REGION.amazonaws.com/YOUR_ACCOUNT_ID/YOUR_REQUEST_QUEUE_NAME"
sqs_response_url: "https://sqs.YOUR_REGION.amazonaws.com/YOUR_ACCOUNT_ID/YOUR_RESPONSE_QUEUE_NAME"

# S3 Bucket - Replace with your actual S3 bucket name
s3_bucket_name: "your-code-submission-bucket"

# Database Endpoint - Replace with your actual RDS endpoint
db_endpoint: "your-database-endpoint.YOUR_REGION.rds.amazonaws.com:5432"

# Backend Server IP - Replace with your actual backend server IP
BACKEND_IP: "YOUR_BACKEND_SERVER_IP"
# ================================
# SETUP INSTRUCTIONS:
# ================================
# 1. Replace YOUR_ACCOUNT_ID with your AWS Account ID
# 2. Replace YOUR_REGION with your AWS region (e.g., us-east-1)
# 3. Replace YOUR_REQUEST_QUEUE_NAME with your SQS request queue name
# 4. Replace YOUR_RESPONSE_QUEUE_NAME with your SQS response queue name
# 5. Replace your-code-submission-bucket with your S3 bucket name
# 6. Replace your-database-endpoint with your RDS endpoint
# 7. Replace YOUR_BACKEND_SERVER_IP with your backend server's public IP
